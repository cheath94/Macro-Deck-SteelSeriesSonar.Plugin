namespace SteelSeriesSonar.Plugin.Models;

/// Represents the volume state used when Sonar Streamer mode is enabled.
/// The API may return this object without populated properties when
/// Streamnode is disabled.

public class StreamVolume
{
}